# Project instructions for Claude Code

Git commits in this repository must never contain `Co-Authored-By` or attribution trailers for
Claude, Anthropic, or any AI assistant. Use only the user's configured Git author identity
(`YANGRUID`). This applies to every commit, in every session, with no exceptions.
